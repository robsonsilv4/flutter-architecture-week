name: architecture
description: An Architecture project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_modular: ^2.0.1
  core_module:
    path: modules/core_module
  login_module:
    path: modules/login_module
  home_module:
    path: modules/home_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
